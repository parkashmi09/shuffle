---
args: environment
---

# Deploy

1. Run `npm run build`
2. Verify `build/` directory was created
3. Report build size and any warnings
4. If environment is "production", remind to check for console.log statements
